<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.pak.mapper.BoardMapper">


	<insert id="enroll">

		insert into psm_board(title, content, writer) values (#{title}, #{content}, #{writer})

	</insert>
 
 	<select id="getList" resultType="com.pak.model.BoardVO">
 	
 	select * from psm_board;
 	
 	</select>
 
 	<select id="getPage" resultType="com.pak.model.BoardVO">
    
        select * from psm_board where bno = #{bno}
    
    </select>
 
  </mapper>